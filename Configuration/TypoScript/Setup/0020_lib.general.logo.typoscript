#-------------------------------------------------------------------------------
#	GENERAL: Logo / preassigned
#-------------------------------------------------------------------------------
lib.general.logo >  ## IMAGE ##
lib.general.logo {
    file = {$logo.file}
    file.height = {$logo.height}
    file.width = {$logo.width}
    stdWrap.typolink.parameter = 1
    stdWrap.typolink.ATagParams = class="pull-left"
}